<mvc:View
    controllerName="eduflowui.controller.Student"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns="sap.m"
    xmlns:smartTable="sap.ui.comp.smarttable"
    xmlns:smartFilterBar="sap.ui.comp.smartfilterbar"
>
    <Page
        id="idPageHome"
        title="{i18n>title}"
    >
        <FlexBox
            justifyContent="Center"
            alignItems="Center"
        >
            <VBox width="95%">
                <smartFilterBar:SmartFilterBar
                    id="sfbStudents"
                    entitySet="Users"
                    showGoOnFB="true"
                    enableBasicSearch="true"
                />
                <smartTable:SmartTable
                    id="stStudents"
                    entitySet="Users"
                    tableType="Table"
                    smartFilterId="sfbStudents"
                    header="{i18n>orders}"
                    enableAutoBinding="true"
                    class="sapUiResponsiveContentPadding"
                    enableExport="true"
                    beforeExport="onSmartTableBeforeExport"
                    enableCopy="false"
                    showFullScreenButton="true"
                    requestAtLeastFields="Status"
                >
                    <smartTable:customToolbar>
                        <OverflowToolbar design="Transparent">
                            <ToolbarSpacer />
                            <Button
                                text="{i18n>approveButtonText}"
                                type="Accept"
                                press="onApproveButtonPress"
                            />
                            <Button
                                text="{i18n>rejectButtonText}"
                                type="Reject"
                                press="onRejectButtonPress"
                            />
                        </OverflowToolbar>
                    </smartTable:customToolbar>
                    <Table
                        id="tblStudents"
                        sticky="ColumnHeaders,HeaderToolbar"
                        mode="MultiSelect"
                        growing="true"
                        growingThreshold="100"
                    >
                    </Table>
                </smartTable:SmartTable>
            </VBox>
        </FlexBox>
    </Page>
</mvc:View>
